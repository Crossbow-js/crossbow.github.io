This error occurs when you try to provide a custom reporter, but the value you provide
is not a string or function.

Only Strings (that are file paths) and Functions are supported as valid reporters. 